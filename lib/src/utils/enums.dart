/// Enumeración de tamaños estandarizados de íconos.
enum DSSize { xs, small, medium, large, xl }

/// Enumeración de colores semánticos de íconos.
enum DSIconColor { primary, secondary, onPrimary, onSecondary, disabled }

/// Variantes de color y estilo del botón.
enum DSButtonVariant { primary, secondary, disabled }
enum DSButtonIconDirection { left, rigth, }

enum DSIconButtonVariant { primary, secondary, disabled }

enum DSFabVariant { primary, secondary }

enum DSSizeRadius { none, xs, small, medium, large, xl, radiusCircular }
enum DSSpacin { none, xxs, xs, small, medium, large, xl, xxl }
