import 'package:pragma_design_system/src/tokens/ds_radius.dart';

/// Fundaciones de radios para el sistema de diseño.
/// Expone los valores de radio base para esquinas redondeadas semánticas.
class DSRadiusFoundations {
  DSRadiusFoundations._();

  static const double none = DSRadius.none;
  static const double radiusXS = DSRadius.radiusXS;
  static const double radiusSM = DSRadius.radiusSM;
  static const double radiusMD = DSRadius.radiusMD;
  static const double radiusLG = DSRadius.radiusLG;
  static const double radiusXL = DSRadius.radiusXL;
  static const double radiusCircular = DSRadius.radiusCircular;
}
